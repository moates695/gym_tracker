import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
  button: {
    padding: 3,
    borderRadius: 2,
    borderWidth: 2,
    borderColor: 'red',
    minWidth: 100,
    alignItems: 'center'
  },
  redBorder: {
    borderColor: 'red',
  },
  greenBorder: {
    borderColor: 'green',
  }
})